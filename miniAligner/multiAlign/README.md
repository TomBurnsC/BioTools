Progressive multiple sequence alignment implementation using Python fundamentals 

Pairwise sequences are  scored and ranked by similarity (guide tree),
then combined in order of decreasing similarity using Needleman-Wunsch,
either sequence-to-sequence, profile-to-sequence, or profile-to-profile,
until all sequences are merged into a single alignment group. 
